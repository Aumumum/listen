import { Scroller } from '../../scroller';
declare const Check_base: import("../../interfaces").IBaseAdapterProcess;
export default class Check extends Check_base {
  static run(scroller: Scroller): void;
}
export {};
